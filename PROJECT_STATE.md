# Текущее состояние работы

## Публикация последних исправлений, 2026-09-17

- Владелец разрешил push в production. Рабочая ветка `dev`, production `master`.
- В публикацию входят исправление сохранения срока, сворачивание редакторов,
  локальные иконки Lucide, описание события Google Calendar и обновлённый бэклог.
- Проверено: четыре целевых теста срока, календаря, иконок и мобильного редактора;
  `git diff --check`. После fetch обе удалённые ветки совпадают с исходным HEAD.
- `setup-context-mode.md` не включён. Данные задач и облачные файлы не менялись.
- Не проверено: завершение деплоя Cloudflare и сохранение события в аккаунте Google.
- Следующий шаг: проверить результат push по удалённым коммитам и дождаться деплоя.

## Описание события Google Calendar, 2026-09-17

- Цель: повторить текст задачи в описании события. Папка
  `C:\_Fork Projects\Sorter-pomogator`, ветка `dev`.
- Изменены `sorter 2025/gtd-task-tools.js`, `tests/gtd-task-tools.test.cjs`,
  `must-do.md` и `PROJECT_STATE.md`. Другие незакоммиченные изменения сохранены.
- Владелец подтвердил на телефоне сохранение срока и сворачивание редактора.
- Описание использует тот же текст, что заголовок. История GTD не добавляется.
- Проверки: целевой тест календарной ссылки, синтаксис скрипта и HTTP обеих страниц.
  Результаты приведены в итоговом сообщении этого шага.
- Не проверено: сохранение нового описания в аккаунте Google Calendar.
- Следующий шаг: пользовательская проверка описания. Push не выполнялся.

## Актуализация must-do, 2026-09-17

- Цель: обновить обязательный бэклог после исправления срока и добавления иконок.
  Папка `C:\_Fork Projects\Sorter-pomogator`, ветка `dev`.
- Изменены только `must-do.md` и `PROJECT_STATE.md` в рамках этого шага.
- Уточнены MD-01 и MD-08, результат проверки 390×340, наличие общего icons.js
  и отсутствие проверки настоящей мобильной клавиатуры. Закрытие GTD не закрыто.
- Статусы публикации сверены с `origin/master` на `a69ed89`.
  Новые исправления срока и иконки остаются незакоммиченными в dev.
- Для будущих snooze и подсказок отмечены уже существующие основы `t:` и @/+.
  Исходные требования и все номера MD сохранены; новые функции не реализовывались.
- Проверено: сверка документа с Git, кодом и записями ранее выполненных проверок.
  Не проверено: новые функциональные сценарии и Cloudflare; шаг только документальный.
- Следующий шаг: проверка владельцем и отдельное разрешение на публикацию.

## Иконки меню и сворачивание обеих страниц, 2026-09-17

- Цель: заменить надпись "Свернуть" значком Lucide и добавить значки меню ⋮.
  Папка `C:\_Fork Projects\Sorter-pomogator`, ветка `dev`.
- Изменены `sorter 2025/tasks.html`, `tasks.css`, `index.html`, `style.css`,
  `tests/task-edit-due.test.cjs` и `PROJECT_STATE.md`.
  Добавлены `sorter 2025/icons.js` и `tests/task-icons.test.cjs`.
- Выбранные SVG взяты из официального репозитория Lucide и встроены локально.
  Лицензионные уведомления сохранены в icons.js; внешнего скрипта Lucide нет.
  Общий render используется в динамическом меню, статические значки заполняются
  при загрузке. Набор имён ограничен локальным списком.
- Кнопка сворачивания использует minimize-2 и доступное имя "Свернуть редактор".
  На странице списка она добавлена к полному списку и полю вопроса сравнения.
  Обе страницы используют существующий общий обработчик без копирования логики.
- Меню ⋮ сохраняет подписи и команды. Все восемь действий активной задачи
  имеют значки 20 px; завершение и игнорирование меняют значок по состоянию.
- Пройдены тесты icons, срока, мобильного возврата и панелей, проверен синтаксис
  встроенных скриптов. В браузере проверены меню и кнопки при ширине 390 px
  и высоте 340 px. Обе кнопки имеют область 44×44 px и остаются в видимой области.
  После сворачивания текст сохранён. Временный размер браузера сброшен.
- Дизайн проверялся как ограниченное изменение через impeccable; интерфейс
  и поведение команд сохранены. Детектор работал без HTML-парсера и сообщил
  прежние предупреждения о шрифте Inter.
- Не проверено: настоящая мобильная клавиатура, отдельное взаимодействие с полем
  вопроса сравнения и все обратные состояния меню на реальном телефоне.
- Следующий шаг: проверка владельцем. Push, merge и production не выполнялись.

## Сохранение срока и сворачивание редактора, 2026-09-17

- Цель: MD-01 и MD-08, папка `C:\_Fork Projects\Sorter-pomogator`, ветка `dev`.
- Изменены `sorter 2025/tasks.html`, `tasks.css`, `mobile-edit-mode.js`,
  `tests/mobile-edit-return.test.cjs`, `must-do.md`, `PROJECT_STATE.md`.
  Добавлен `tests/task-edit-due.test.cjs`. Посторонний `setup-context-mode.md` не менялся.
- Календарь обновляет черновик при input и change, повторная отрисовка использует
  срок черновика. Перед сохранением проверяется текущая дата календаря.
  Принудительный возврат фокуса в textarea после выбора даты убран.
- В мобильном увеличенном редакторе задачи добавлена кнопка "Свернуть".
  Общий обработчик снимает увеличенный режим и фокус, но сохраняет текст черновика.
  Сохранение и отмена остаются отдельными действиями. Сортировка не менялась.
- Пройдены целевые тесты срока, мобильного возврата и мобильных панелей.
  В локальном браузере проверено сохранение срока, повторное открытие и очистка.
  Проверка проводилась на localhost developer-списке из десяти числительных;
  тестовый срок убран, рабочий список и Dropbox не использовались.
- Не проверено: мобильная отрисовка и настоящая клавиатура. В текущем подключении
  браузера изменение viewport недоступно через запрошенную CDP capability.
  Механическая проверка дизайна работала без HTML-парсера и сообщила только
  прежние предупреждения о шрифте Inter; это не подтверждает визуальное качество.
- Следующий шаг: пользовательская проверка MD-01 и MD-08. Push и merge не выполнялись.

## Обязательный бэклог, 2026-09-17

- Цель: собрать запросы владельца и оставшуюся работу в `must-do.md`.
- Папка: `C:\_Fork Projects\Sorter-pomogator`, ветка `dev`.
- Изменены только `must-do.md` и `PROJECT_STATE.md` в рамках этого шага.
  Ранее незакоммиченные изменения кода сохранены без вмешательства.
- Повторы объединены. Назначение срока GTD отделено от новой ошибки сохранения
  срока в редакторе задачи. Счётчик архива отделён от подсказок ввода.
- Статусы сверены с `BACKLOG.md`, текущим состоянием работы и кодом GTD.
  Локальная реализация не приравнивается к публикации или мобильной проверке.
- Не проверено: воспроизведение новой ошибки срока и выполнение новых запросов.
  Этот шаг меняет только документацию; функциональные тесты не запускались.
- Следующий шаг: выбрать отдельную задачу для реализации, первым кандидатом
  указан срок в редакторе. Push, merge и production не выполнялись.

## Даты GTD, календарь, защита GTD и проверка моста, 2026-09-17

- Работа выполнена в `C:\_Fork Projects\Sorter-pomogator`, ветка `dev`.
  Предыдущие незакоммиченные изменения меню сохранены. Production не менялся.
- Добавлены `sorter 2025/gtd-task-tools.js`, `tests/gtd-task-tools.test.cjs`.
  Изменены `process.html`, `unsaved-changes.js`, `app.js`, `task-format.js`,
  `task-format.css` и подключения в `index.html` и `tasks.html`.
- GTD сохраняет выбранный срок как `due:`, отличает его от даты создания,
  сохраняет дату создания старой задачи и присваивает дату новой задаче общей
  функцией `prepareNewTask`. Повторное сохранение обновляет ту же задачу.
- Итоговый экран позволяет изменить срок и открыть заполненную форму Google
  Calendar. Событие занимает выбранный день, сохранение подтверждает пользователь
  в Google. В Google передаются только название и дата, не исходная история.
- Google описывает этот способ в документации:
  https://developers.google.com/workspace/calendar/api/concepts/inviting-attendees-to-events
- Новая партия из Insert Unsorted получает дату через `prepareNewTask` на этапе
  ввода, без изменения алгоритма сравнений. Быстрое добавление уже использует
  ту же функцию. Сырой редактор намеренно не переписывает строки автоматически,
  поскольку создание нельзя надёжно отличить от изменения существующей строки.
- Дата создания отображается без фоновой плашки и жирного начертания. Стиль
  срока не менялся. На localhost developer-списке проверено добавление одной
  временной задачи; временная задача и созданный ею маркер убраны после проверки.
- GTD зарегистрирован в общей защите правок. Предупреждение учитывает поля,
  результат и промежуточные ответы; успешное сохранение обновляет состояние.
  Начало другого разбора также требует подтверждения, если есть правки.
- Проверены тесты GTD, защиты, операций, рендера, форматирования, модели списка,
  сортировочного контура, AI worker, обмена и браузерного Dropbox-адаптера.
  Проверен синтаксис изменённых JS и встроенных скриптов трёх страниц.
- В браузере проверены календарный путь и предупреждение GTD, сохранение старой
  даты создания, UI при 390×844 и настольной ширине. HTTP трёх страниц отвечает 200.
- Мост читает `/ai/requests/<id>.txt`, вызывает OpenClaw локально и сохраняет
  `/ai/results/<id>.txt`. Протокол не содержит ключей. Тесты используют имитации,
  реальных поручений не отправлялось.
- Файл секретов моста существует в `%LOCALAPPDATA%\ItemSorterAiBridge\credentials.json`,
  не отслеживается Git. Windows ACL наследуется; широких разрешений для проверенных
  общих групп не найдено. Это не защита от программ текущего пользователя.
- Dropbox-токены браузера хранятся в localStorage и доступны скриптам страницы.
  Внешние CDN и возможный XSS остаются риском. Запрет действий в prompt OpenClaw
  не является технической изоляцией его файловых и внешних инструментов.
- Процесс моста из `worker.lock` не существует. Мост не запускался: он может
  обработать накопившиеся задания и потратить лимит. Для запуска нужно разрешение.
- Не проверено: настоящее создание события в Google, доставка через живой мост,
  физический мобильный браузер. Проверка списка процессов через CIM была запрещена,
  статус PID отдельно проверен через Get-Process.
- Следующий шаг: пользовательская проверка и решение о запуске моста. Push и
  merge не выполнялись.

## Меню задач и защита правок, 2026-09-17

- Папка: `C:\_Fork Projects\Sorter-pomogator`, ветка `dev`.
- Изменены `sorter 2025/tasks.html`, `tasks.css`, `index.html`, `dropbox.js`;
  добавлены `task-action-menu.js`, `unsaved-changes.js` и
  `tests/task-menu-unsaved.test.cjs`.
- Стороннее меню по правому клику отключено. Его действия доступны через ⋮.
  Нативное меню выделения текста не перехватывается. Меню ⋮ ограничено видимой
  областью экрана, имеет непрозрачный фон и внутреннюю прокрутку.
- Обе основные страницы предупреждают перед потерей черновика. Перезагрузка
  и закрытие используют стандартное предупреждение браузера; внутренние ссылки
  используют диалог с возможностью остаться. Несинхронизированные локальные
  изменения также учитываются при закрытии и замене версии.
- Загрузка Dropbox и восстановление истории проверяют правки до замены.
  Подтверждение ручной загрузки теряет силу, если документ или черновик изменился
  за время запроса. Отмена не меняет список, резервную копию или состояние sync.
- Пройдены `task-menu-unsaved.test.cjs`, `mobile-edit-return.test.cjs`,
  `mobile-panels.test.cjs`, `runtime-mode.test.cjs`; проверен синтаксис новых
  скриптов, `dropbox.js` и встроенных скриптов обеих страниц.
- Браузерная проверка developer-режима: 1440×900 и 390×340. Меню удержано в
  пределах экрана; при малой высоте доступна прокрутка. Копирование и вырезание
  текста работают. После вырезания всего текста уход вызывает диалог защиты.
  Тестовые правки отменены, рабочие и облачные данные не изменялись.
- Не проверено: физический мобильный браузер с настоящей клавиатурой,
  стандартное предупреждение при закрытии вкладки вручную и реальная загрузка
  Dropbox. Последняя проверена только на имитации ответа без сетевого запроса.
- Область защиты: две основные страницы. Отдельный экран GTD этим шагом не менялся.
- Следующий шаг: пользовательская проверка. Push, merge и production не выполнялись.

## Цель

Текущий шаг: меню задач без перехвата работы с текстом и защита несохранённых
правок на двух основных страницах. Изменения локальны в `dev`; push и merge
для этого шага не выполнялись.

## Папка и ветка

- Папка: `C:\_Fork Projects\Sorter-pomogator`
- Ветка: `dev`
- Production: `origin/master` (`37a25ff`)
- Рабочая ветка: `dev`, первый этап сохранён локальным коммитом и не отправлен.

## Файлы текущего набора

- восстановление и совместимость: `sorter 2025/app.js`,
  `sorter 2025/markers.js`, `sorter 2025/task-list-operations.js`;
- подключение актуальных версий скриптов: `sorter 2025/index.html`,
  `sorter 2025/tasks.html`;
- правила и документация: `AGENTS.md`, `BACKLOG.md`, `Project Structure.md`,
  `docs/SORTING_BEHAVIOR_SOURCE_OF_TRUTH.md`,
  `docs/agent-rules/sorting.md`, `PROJECT_STATE.md`;
- тесты: `tests/sorting-pipeline.test.cjs`,
  `tests/real-list-sorting-smoke.test.cjs`,
  `tests/task-list-model.test.cjs`.

Локальный `setup-context-mode.md` не относится к восстановлению сортировки и
намеренно не включается в набор для публикации.

## Восстановленные функции

Функции ниже восстановлены из `99cf747`. После восстановления команды
`mergeArraysUI()`, `sortTasks()`, `filterTasksUI()` и финальная сборка
`insertUnsortedTasksUI()` отдельно адаптированы по решениям владельца:

- `mergeSort`, `merge`, `compareTasks`;
- `parseArrays`, `gallopRight`, `gallopLeft`, `mergeGalloping`;
- `filterTasks`, `filterTasksUI`;
- `insertUnsortedTasksUI`;
- `heapifyDown`, `partialSortTasks`, `parseAllSections`,
  `promoteTailCandidates`, `sortTasks`.

`parseArrays()` больше не вызывается рабочей кнопкой и оставлена только как
историческая характеристика. `mergeArraysUI()` теперь читает пять списков через
`parseTaskDocument()`, сливает только `sorted + inboxSorted`, записывает через
`serializeTaskDocument()` и удаляет опустевший маркер.

Подтверждённо мёртвые и некорректные `quickSort`, `binaryInsert` и
`sortTasksv1` удалены с прямого разрешения владельца. Они не входили в дерево
вызовов даже в `99cf747` и самом первом коммите `09fa69b`.

Более поздние функции добавления задач, дат создания, Dropbox и страницы задач
не откатывались.

## Выполненные проверки

- Финальные целевые проверки 2026-09-17 пройдены: синтаксис `app.js` и
  `markers.js`, форматирование, модель списков, операции одной задачи и
  сортировочный pipeline.
- `Sort Tasks` больше не вызывает назначение приоритетов старому `SORTED`;
  отсутствие этого побочного вызова закреплено тестом.

- Общий `serializeTaskDocument()` теперь выводит секции в порядке
  `inboxUnsorted`, `INBOX SORTED`, `SORTED`, `PARTIALLY SORTED`,
  `IGNORED TASKS`; отдельная отсортированная партия видна выше основного списка.
- `insertUnsortedTasksUI()` сохраняет исторические сравнения и позиции задач,
  но собирает итог через общий `formatTaskList()`, поэтому между соседними
  задачами остаётся ровно одна пустая строка.

- `sortTasks()` переведена на действующую пятисписочную модель: активные
  `inboxUnsorted` сортируются в `inboxSorted`, `sorted` остаётся неизменным,
  остаток партии больше 60 задач ставится перед прежним `partiallySorted`.
- Выполненные строки из входящих, дубликаты и ignored-задачи сохраняются.
- При уже заполненном `INBOX SORTED` или нескольких ignored-блоках операция
  останавливается до сохранения и сравнений.
- `node --check` для `app.js` и `markers.js` — пройден.
- `format-task-list.test.cjs`, `task-list-model.test.cjs` и
  `sorting-pipeline.test.cjs` — пройдены. Синтетические проверки покрывают
  обычную партию, дубликаты, выполненную задачу, блокировки и партию из 61
  задачи.
- Независимый read-only review существенных дефектов не нашёл; найденные им два
  пробела тестов закрыты синтетическими сценариями.
- Локальный сервер отвечает `200` для `/` и `/tasks.html`.

- На ширине 1440 px воспроизведена причина скачка: общий мобильный обработчик
  включался на ПК и прокручивал страницу на 58 px для добавления и на 90 px для
  поиска.
- Мобильный режим редактора ограничен тем же breakpoint `760px`, который
  использует его CSS; при переходе на ПК-ширину активный мобильный режим
  очищается.
- В `mobile-edit-return.test.cjs` добавлен контракт: мобильный фокус сохраняет
  прежнее поведение, а ПК-фокус не включает компактный режим и не прокручивает
  страницу.
- В браузере после исправления оба ПК-поля проверены при ширине 1440 px:
  `scrollY` остаётся равным `0`; при ширине 390 px режимы добавления и поиска
  продолжают занимать доступную мобильную область.
- Все 12 файлов `tests/*.test.cjs` пройдены одним прогоном после исправления.
- Проверка интерфейсных антипаттернов не нашла совпадений; инструмент работал в
  ограниченном режиме без модулей полноценного разбора HTML и CSS.

- До первой согласованной адаптации все 16 восстановленных тел были программно
  сравнены с `99cf747` и совпадали посимвольно.
- `node --check "sorter 2025/app.js"` — пройден.
- `node tests/format-task-list.test.cjs` — пройден.
- `node tests/task-list-model.test.cjs` — пройден.
- `node tests/sorting-pipeline.test.cjs` — пройден.
- `node tests/task-list-operations.test.cjs` — пройден.
- Все восемь файлов `tests/*.test.cjs` запущены одним прогоном — пройдены.
- `node tests/real-list-sorting-smoke.test.cjs` — пройден на всём приватном
  списке: 57 сортируемых задач, 30 строк ignored-tail, 322 сравнения и 86
  решений фильтра. Тексты задач тест не выводит.
- SHA-256 приватного файла до и после прогона совпал; файл не изменён, закрыт
  `.gitignore` и не отслеживается Git.
- `tests/sorting-pipeline.test.cjs` заменён на characterization-тесты поведения
  рабочего эталона `99cf747`: границы секций, последовательности сравнений,
  остановка частичной сортировки и итоговая сборка четырёх команд.
- Из `sorter 2025/task-list-operations.js` и его exports удалён ошибочный
  параллельный контур массовой сортировки: `stableMergeSort`,
  `mergeRankedLists`, локальный `heapifyDown`, `extractTopRanked`,
  `sortIncomingIntoMain`, `promotePartialIntoSorted`, `mergeInboxSorted`,
  `insertTasksIntoSorted`, `getFilterCandidates` и `rebuildAfterFilter`.
- После удаления параллельного контура и до адаптации Merge все 16 действующих
  функций были повторно сравнены с `99cf747`.
- Создан обязательный источник истины
  `docs/SORTING_BEHAVIOR_SOURCE_OF_TRUTH.md`: он описывает эталонный коммит,
  дерево вызовов, все 16 функций, точные контракты четырёх команд, исторические
  ограничения и порядок будущей пофункциональной адаптации.
- `AGENTS.md` направляет агента сначала в источник истины, затем в правила
  безопасного изменения.
- Из `Project Structure.md` и `docs/agent-rules/sorting.md` удалены утверждения
  сломанного рефакторинга, противоречившие восстановленному коду.
- Канонический маркер `inboxSorted` изменён на `INBOX SORTED`; точный legacy-
  маркер `NEW ARRAY` продолжает читаться и автоматически мигрирует при записи.
  Вхождения внутри текста задач не изменяются, перед миграцией создаётся снимок.
- `Merge Arrays` проверена на синтетическом документе со всеми пятью списками:
  последовательность сравнений сохранена, объединяются только два нужных
  массива, остальные списки и выполненные задачи сохраняются, пустой маркер
  удаляется.
- `node --check` для `markers.js` и `app.js`, а также
  `format-task-list.test.cjs`, `task-list-model.test.cjs` и
  `sorting-pipeline.test.cjs` пройдены после адаптации.
- `real-list-sorting-smoke.test.cjs` пройден на приватном списке в памяти:
  87 исходных строк, 320 сравнений, исходный файл не изменён.
- Репозиторий добавлен в глобальный `safe.directory` Git только по точному пути;
  обычный `git status` теперь работает без одноразового обхода.

## Не выполнено

- Ручной интерактивный прогон `Sort Tasks` владельцем в `mode=developer`.
- Полный smoke-test на приватном реальном списке не запускался: для полного
  прогона требуется отдельное разрешение.
- Управляемая проверка во встроенном браузере не запустилась из-за ошибки
  подключения браузерного инструмента; сервер и обе страницы доступны.

## Следующий шаг

Владелец повторно проверяет `Sort Tasks` и `Insert Unsorted Tasks` на
developer-списке. Push и merge требуют отдельной команды владельца.

## Изолированный developer mode (текущая работа)

- Добавлен общий `runtime-mode.js`: он не копирует функции приложения, а
  направляет их чтение и запись либо в рабочий `tasks`, либо в отдельный
  `sorter_dev_tasks`.
- Developer mode доступен только локально по `?mode=developer`, сохраняется при
  переходах между основным списком, страницей задач и GTD-разбором.
- Первый запуск создаёт отдельный список из десяти нейтральных
  задач-числительных с пустыми строками между задачами. Рабочий список при этом
  не читается и не изменяется.
- Dropbox в developer mode заблокирован до получения токена и сетевого вызова;
  интерфейс показывает компактный статус `DEV · Dropbox off`.
- Добавлен `tests/runtime-mode.test.cjs`: проверяет разделение ключей, запрет
  developer mode на production, сохранение параметра в маршрутах и отсутствие
  Dropbox-вызовов.
- Синтаксис изменённых JS-файлов и целевые тесты сортировки, модели списка,
  форматирования и приватный smoke-test пройдены. Приватный файл не изменён.
- В браузере подтверждены точные десять developer-задач, десять строк на
  `/tasks.html`, сохранение `mode=developer`, статус `DEV · Dropbox off` и
  неизменность текста в уже открытой обычной вкладке.
- Обе страницы визуально проверены при `1280×720` и `390×844`: developer-статус
  виден, горизонтального переполнения нет. UI-детектор выполнен в degraded-
  режиме из-за отсутствующих parser-модулей; он сообщил только о ранее
  существовавшем использовании Inter.

## Видимость сортировочных команд

Четыре исходные пользовательские команды `Insert Unsorted Tasks`, `Sort Tasks`,
`Filter Tasks` и `Merge Arrays` находятся в постоянно видимом блоке
`.primary-actions`. На мобильном экране они образуют компактную сетку 2×2.
`Filter Tasks` и `Merge Arrays` нельзя снова переносить в закрытый блок «Ещё
инструменты» без прямого решения владельца. После адаптации пометка
`(не работает)` у Merge Arrays удалена.

## Канонический вывод Sort Tasks

- `sortTasks()` больше не записывает полный документ через исторический
  `parts.join('\n')`; итог проходит через общий `formatTaskList(parts)`.
- Алгоритм сравнений, границы входных секций, порядок `winners + sorted`,
  формирование `partiallySorted` и сохранение ignored-хвоста не менялись.
- Целевой characterization-тест требует одну пустую строку между соседними
  задачами и одновременно проверяет прежнюю последовательность сравнений.
- Приватный smoke-test сравнивает содержимое ignored-хвоста без старой раскладки
  пустых разделителей: порядок и все непустые строки обязаны сохраниться, а
  разделители теперь намеренно приводятся к каноническому виду.

## Аудит нескольких ignored-секций

- Текущая модель `ignored: []` плюс единственный `markers.ignored` не способна
  представить два независимых ignored-блока. `filterTasksUI()` дополнительно
  удаляет все маркеры и полностью уплощает документ; текущая сериализация также
  схлопнет несколько ignored-блоков в один.
- Единая политика удаления и перенумерации приоритетов вынесена в `BACKLOG.md`;
  в текущей работе приоритеты менять нельзя.
- Владелец отложил поддержку нескольких ignored-секций. Текущий целевой
  контракт использует один блок; новые ignored-задачи добавляются в его начало.
  При нескольких маркерах Filter Tasks останавливается до записи.
- Зафиксировано общее правило: сортировка и фильтрация не могут автоматически
  терять завершённые задачи, дубликаты или другие экземпляры задач. Новые
  задачи аналогично добавляются в начало `inboxUnsorted`.

## Filter Tasks: текущая адаптация

- Функция больше не удаляет маркеры и не превращает документ в плоский массив:
  каждый оставленный или завершённый экземпляр сохраняет исходную секцию.
- Уже завершённые и существующие ignored-задачи не показываются в диалогах;
  одинаковые строки обрабатываются и сохраняются как отдельные экземпляры.
- Новые ignored-задачи добавляются перед старыми. `Удалить` остаётся явным
  разрешённым удалением конкретного экземпляра.
- Добавлено действие `Сделано`. Оно вызывает переиспользуемую
  `TaskListOperations.setTaskCompletion()`, которая сохраняет приоритет, дату
  создания и остальной исходный текст, добавляя todo.txt-префикс с локальной
  датой выполнения.
- Диалог Filter Tasks показывает четыре явных действия одной группой:
  `Оставить`, `Игнорировать`, `Сделано`, `Удалить`. На мобильном экране
  390×844 они проверены в сетке 2×2 без горизонтального переполнения.
- Контрольный UI-детектор работал в degraded-режиме из-за отсутствующих
  parser-модулей и указал только на ранее существующую анимацию `width` в
  индикаторе прогресса другого диалога сортировки.
- Канонический маркер теперь `IGNORED TASKS (YYYY.MM.DD)`; русский вариант и его
  дата читаются и мигрируют при записи.

## Контрольная точка перед объединением в production

- Полный локальный набор из десяти тестовых файлов пройден 2026-09-11.
- Приватный smoke-test выполнен без изменения исходного файла.
- Рабочая ветка для этого набора изменений — `dev`; она объединяется с
  `master` только fast-forward без переписывания истории.
- Push и фактический деплой не входят в эту контрольную точку и требуют
  отдельной команды владельца.

## Мобильные панели и поиск над клавиатурой

- Исправлены несуществующие CSS-токены мобильных панелей: поиск, добавление и
  настройки используют непрозрачные поверхности `--bg-el` и `--border-h`.
- Поиск подключён к существующему event-driven `visualViewport`-режиму через
  `data-mobile-editor-layout="search"`; отдельный опрос и второй контроллер
  клавиатуры не создавались.
- В режиме поиска шапка, FAB и нижняя панель скрываются, а оставшаяся высота
  делится между непрозрачной строкой поиска и прокручиваемыми результатами.
- Панель настройки списка стала непрозрачным прокручиваемым нижним листом с
  закреплённой шапкой и затемнением фона. FAB скрывается, пока лист открыт.
- В браузере проверены 390×844 и уменьшенная область 390×400: фон панелей
  непрозрачный, при высоте 400 px список получает 316 px, перекрытий нет.
- Desktop 1280×720 визуально не изменился. Добавлен контрактный тест
  `tests/mobile-panels.test.cjs`.
- UI-детектор выполнился в degraded-режиме без parser-модулей и сообщил только
  о ранее существующем шрифте Inter; новых предупреждений по панелям нет.

## Автоматические контексты

- Общая логика вынесена в `sorter 2025/auto-context.js`: распознавание
  URL-ключа `auto`, выбор `И/ИЛИ`, определение устройства и локальный
  расчёт расстояния до дома.
- Контексты в `tasks.html` теперь можно выбирать по нескольку. В режиме
  `ИЛИ` достаточно любого из них, в режиме `И` задача должна содержать все.
- Ключ распознаётся в `/tasks/auto`, `/tasks?auto` и `/tasks.html?auto`. Cloudflare-
  переход из `/tasks/auto` описан в `sorter 2025/_redirects`; локальная навигация
  нормализуется `runtime-mode.js`.
- Во вкладке `Ещё инструменты` главной страницы добавлена кнопка
  `Задать координаты дома`. Координаты хранятся в `localStorage` текущего
  origin и не попадают в task-документ или Dropbox.
- Авторежим опрашивает геопозицию один раз при открытии и только если домашняя
  точка уже задана. При отказе или неточном ответе остаётся только контекст
  устройства; место не угадывается.
- Целевой тест: `node tests/auto-context.test.cjs`; изоляция developer-mode
  дополнена в `tests/runtime-mode.test.cjs`.
- Пройдены `auto-context.test.cjs`, `runtime-mode.test.cjs`,
  `mobile-panels.test.cjs`, syntax-check нового модуля и `git diff --check`.
- В локальном браузере проверены `tasks.html?mode=developer&auto=1`
  при 390×844 и 1280×720, а также кнопка в `Ещё инструменты`. Ошибок
  консоли нет. Реальное разрешение на геопозицию не выдавалось, чтобы не
  сохранять тестом точные координаты владельца.

## Возврат к задаче после мобильного редактирования

- Причина потери первого нажатия: между `pointerdown` и `click` происходил
  `focusout`; мобильная раскладка успевала свернуться и сдвинуть кнопку до
  завершения того же касания.
- `sorter 2025/mobile-edit-mode.js` теперь удерживает компактную раскладку
  до фактического `click` по внутренней кнопке. Правило переиспользуется всеми
  экранами с `data-mobile-editor`.
- `saveEdit()` и `cancelEdit()` после полного `render()` повторно находят ту же строку
  по индексу и возвращают её в видимую область после изменения `visualViewport`.
- Ручной браузерный тест выполнен на отдельном origin `localhost` при 390×844
  и 390×420: одно нажатие закрыло редактор, изменило текст, а строка осталась
  видимой (`top=331`, `bottom=394` при высоте 844). Тестовый текст возвращён.
- Регрессионный контракт закреплён в `tests/mobile-edit-return.test.cjs`.
- Пройдены `mobile-edit-return.test.cjs`, `mobile-panels.test.cjs`,
  `auto-context.test.cjs`, syntax-check `mobile-edit-mode.js` и `git diff --check`.
- UI-детектор запущен после изменения. Он работал в degraded-режиме без
  parser-модулей и отметил только ранее существовавшие стили `process.html`; новых
  предупреждений по сценарию сохранения нет.

## Настраиваемый порядок частей задачи

- `TaskFormat.renderTaskHtml()` стал единым компоновщиком содержимого задачи и
  метаданных. Параметр `leadingMeta` переносит только названные группы наверх,
  не меняя сырой текст todo.txt и не дублируя метаданные.
- Страница `/tasks` передаёт `leadingMeta: ['contexts', 'hashtags']` для обычного
  списка и диалога сравнения. Контексты и хештеги показываются над текстом;
  приоритет, проекты, даты и остальные поля остаются под ним.
- Другие страницы сохраняют прежний порядок. Они могут выбрать другой порядок
  тем же параметром без отдельного рендерера.
- Изменены `sorter 2025/task-format.js`, `tasks.html`, `tasks.css`, версии общего
  скрипта в `index.html` и `process.html`, а также `tests/task-format.test.cjs`.
- Пройдены syntax-check `task-format.js`, `task-format.test.cjs`,
  `mobile-edit-return.test.cjs` и `git diff --check`.
- В developer-mode проверены 390×844 и 1280×720. Метки находятся над текстом,
  остальные данные под ним, повторов и переполнения нет.
- UI-детектор выполнен один раз. Он работал в degraded-режиме без parser-модулей
  и отметил только ранее существующий шрифт Inter; новых замечаний нет.
- Следующий шаг: остановиться перед commit и push.
